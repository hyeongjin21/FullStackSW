1. cd "C:\Users\smhrd\Desktop\study"
2. git init
3. git remote add origin https://github.com/hyeongjin21/FullStackSW.git
4. git add .
5. git commit -m "커밋 메세지"

git pull origin main

git branch -M main             # 브랜치 이름이 main이 아니라면 변경
git push -u origin main        # GitHub에 푸시




[Github 최초 설정,저장,업로드 명렁어 순서]
git init

git add 파일명 or 폴더명 or .(전체)

git commit -m "저장할 메시지 내용 입력"

git branch -M main

git remote add origin https://github.com/username입력/react_study2025.git

git push -u origin main

[저장, 업로드 명령어 순서]
git add 파일명 or 폴더명 or .(전체)

git commit -m "저장할 메시지 내용 입력"

git push -u origin main